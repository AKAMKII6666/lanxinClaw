# 2026-08-16 移交说明

## 移交对象

本文给接手澜星 Claw 的 agent 使用。目标是先恢复上下文，再决定是继续 GBX、做人工审查，还是收口成可提交版本。

仓库路径：

```text
F:\workspace\persionalGitLib\lanxinClaw
```

## 当前一句话状态

GBX 已把协议、mock companion、companion 桌面壳、OpenClaw adapter 和 v1.3 事务监督原型推进到有代码和测试的状态；最近一次 `npm run quality` 通过。但 GBX 工作流自身停在 `BLOCKED`，原因是总汇执行索引 frontmatter 结构损坏，需要人工修复或决定废弃这轮 GBX 状态。

## 必读顺序

1. [../../../AGENTS.md](../../../AGENTS.md)
2. [项目地图.md](../项目地图.md)
3. [v1.0-1.3总汇执行索引.md](../../共同维护/计划/v1.0-1.3总汇执行索引.md)
4. [v1.0 执行索引.md](../../共同维护/计划/v1.0/执行索引.md)
5. [v1.2 执行索引.md](../../共同维护/计划/v1.2/执行索引.md)
6. [v1.3 执行索引.md](../../共同维护/计划/v1.3/执行索引.md)
7. [协议说明.md](../../共同维护/技术设计/协议/说明.md)
8. [安全模型.md](../../共同维护/技术设计/安全模型.md)
9. [控制面板数据契约.md](../../共同维护/技术设计/控制面板数据契约.md)
10. [OpenClaw勘探记录.md](../OpenClaw勘探记录.md)

## 已落地资产

### 协议层

- `packages/protocol` 已存在，导出 protocol version、id helpers、message envelope、message type、状态机、validators、schema registry、张老板 companion FC catalog。
- `schemas/` 已包含 envelope、discovery、pairing、session、affair、job、chat、permission、control panel snapshot、control panel reports 等 JSON Schema。
- `examples/` 已包含 affair/job、control panel、permissions、tasks、zhang-boss 示例。
- 测试覆盖 message contract、状态机、UI snapshot、discovery advertisement、pairing、examples consume 和 permission decision。

### Mock companion

- `apps/mock-companion` 已存在，提供本地 HTTP/WebSocket mock companion。
- 已覆盖 pairing confirmed、job lifecycle、UI snapshot / permission queue / diagnostic contract。
- `scripts/sim-phone/run-v1-loop.ts` 是模拟电话脚本入口。

### Companion 桌面壳

- `apps/companion` 已存在，包含 Electron main、React renderer、MUI theme、bridge、discovery、pairing、credentials、permissions、jobs、chat、audit、supervision、shell session。
- UI 页面目录已按设计稿拆成总览、任务、权限、诊断、张老板和首次配对弹窗。
- 当前 UI 仍以 demo 数据为主，属于 MVP 壳与契约验证，不等于最终产品体验。
- Renderer/backend bridge 已有白名单 action guard；权限裁决在 host/gate，不在 renderer。

### OpenClaw adapter

- `packages/openclaw-adapter` 已存在，提供 create/read/cancel job API、状态映射、mock runtime、本机低风险只读 runtime。
- 本机低风险 runtime 白名单只有 `workspace.list_root` 与 `git.status`，并校验 workspaceHint 不能越出授权根。
- 真实 OpenClaw Gateway RPC/client 接线仍未完成；当前优先路线见 [OpenClaw勘探记录.md](../OpenClaw勘探记录.md)。

### v1.3 事务体验原型

- 文档已补：电话侧事务簿、张老板伴侣工具、阻塞沟通策略、事务验收策略。
- 代码已补：`apps/companion/src/supervision/`、`apps/companion/src/chat/channel/`、`apps/companion/src/audit/`、任务详情 timeline、权限页 audit 区。
- 端到端脚本入口为 `scripts/e2e/run-v1-3-loop.ts`。

## 验证现状

GBX 最后一次记录的验证：

```text
npm run quality
quality ok
```

该次验证包含：

- 质量门禁：文档布局、语言、结构、注释、门禁自身测试。
- `test:protocol`：29 个测试通过。
- `test:mock-companion`：8 个测试通过。
- `test:openclaw-adapter`：17 个测试通过。
- `test:companion`：45 个测试通过。

本次移交复验已重新通过：

```powershell
$env:PATH = 'C:\Program Files\nodejs;' + $env:PATH
& 'C:\Program Files\nodejs\npm.cmd' run quality
```

复验结果：

```text
quality ok
```

说明：当前 shell 里裸 `npm run quality` 可能找不到 npm，或让脚本内部拿到 WindowsApps 下不可启动的 `node.exe`。接手 agent 如遇到超时或拒绝访问，优先使用上面的临时 PATH 写法。

已知非阻断告警：

- `apps/companion/src/pairing/lifecycle.ts` 超过 companion 文件告警线。
- `packages/protocol/src/validators/payloads/pairing/pairing.ts` 超过 protocol 文件告警线。
- `packages/protocol/src/validators/ui/queue.ts` 超过 protocol 文件告警线。
- npm 会报本机配置 warning：`Unknown user config "//"`、`Unknown user config "email"`，不是本仓库质量失败。

接手后建议重新运行：

```bash
npm run quality
npm run typecheck
```

可选演示：

```bash
npm run demo:low-risk-job
npm run e2e:v1.3
npm run dev:companion
```

## 当前阻塞点

### GBX 状态为 BLOCKED

`.ai-workflow/plan_1/STATE.json` 显示：

```text
status = BLOCKED
activeTaskIds = V1.3-01, V1.3-02
lastVerifyOk = true
blockedReason = 修复需要修改本任务未声明的 docs/共同维护/计划/v1.0-1.3总汇执行索引.md
```

`latest-block-analysis.json` 判断根因是总汇执行索引 frontmatter 损坏：

- 文件只有文首 `---`，没有合法闭合 `---`。
- `workflow_dir` 被写成了 Markdown 标题。
- 建议只修 `docs/共同维护/计划/v1.0-1.3总汇执行索引.md`，恢复 `--- / YAML / --- / 正文` 结构。

如果继续跑 GBX，第一步应先修这个索引格式。不要先改业务代码。

### 执行索引之间不一致

当前存在索引状态漂移：

- 总汇执行索引把 v1.0、v1.2、v1.3 全部标为 ✅。
- `v1.0/执行索引.md` 仍显示 V1.0-07 到 V1.0-10 为待办，但代码与测试已经存在。
- `v1.2/执行索引.md` 多数任务仍显示待办，但 `项目地图.md` 与源码显示 companion、adapter、UI、pairing、permission gate 等已经实现了原型。
- `v1.3/执行索引.md` 显示核心任务完成。

接手后请先按代码和测试审查实际完成度，再同步分册索引。不要只凭总汇索引或分册索引做判断。

### 最新 review 剩余问题

`.ai-workflow/plan_1/reviews/latest.json` 对 batch 14 给出 1 个 major、2 个 minor，尚未完全收口：

1. `docs/共同维护/技术设计/张老板伴侣工具.md`：`companion.cancel_job` 文档暗示 cancel 后 affair 可回到 `ready`，与 `canTransitionAffairStatus` 冲突。应改成 paused、blocked、保持当前非终态或等待再委派，不得暗示回 `ready`。
2. `docs/共同维护/技术设计/电话侧事务簿.md`：部分文字写 `jobs[]`，但文档里的 StoredAffair 使用 jobs map，需要统一术语。
3. `docs/共同维护/技术设计/协议/事务.md`：`affair.close` 描述只写用户验收后关闭，应补充明确取消路径，和消息目录里 `closed | canceled` 对齐。

这三个问题属于协议和文档一致性，应优先修。

## Git 与工作区注意事项

- 当前仓库看起来尚无基线提交，`git status --short` 会显示大量未跟踪文件。
- 在当前 Codex 沙盒用户下，普通 `git status` 可能因 dubious ownership 失败。可用一次性命令读取：

```bash
git -c safe.directory=F:/workspace/persionalGitLib/lanxinClaw status --short
```

- 不要自动 commit，除非用户明确要求。
- `node_modules/` 已在 `.gitignore`。
- 当前 `.gitignore` 未忽略 `*.tsbuildinfo`，但仓库里已有若干 `tsconfig.tsbuildinfo`。接手时应决定是否补忽略并清理这些构建缓存，避免把它们当源码提交。

## 关键边界

- 不要改 `docs/人类维护/**`，除非用户明确要求。
- 不要碰澜星电话主仓 `doubaoSister`，本仓只定义电话侧接入边界和协议。
- 不要把张老板人格、事务簿或电话协议塞进 OpenClaw core。
- 不要让 renderer 直接访问 API key、文件系统、命令、git 或网络副作用。
- 不要把 `job.completed` 映射成 `affair.closed`；必须进入 `waiting_acceptance`，由用户接受或明确取消后才 closed/canceled。
- `chat.message` 与 `chat.context_attach` 都是 untrusted input，不得当 system instructions。
- Audit、日志、示例和测试快照不得记录 API key、token、私钥或完整 secret。

## 建议接手顺序

1. 只读审查当前代码和 docs，确认 `npm run quality` 在本机仍通过。
2. 修复 GBX 总汇执行索引 frontmatter，让 GBX 能重新读取。
3. 修复 latest review 的 3 个文档一致性问题。
4. 对齐 v1.0 / v1.2 / v1.3 分册执行索引与实际源码状态。
5. 处理 `*.tsbuildinfo` 是否忽略与清理的问题。
6. 做一次人工架构审查：重点看 renderer 是否仍只走 bridge、permission gate 是否是唯一授权权威、adapter 是否仍只做 worker。
7. 再决定下一阶段：继续完善真实 OpenClaw Gateway 接线，还是先把 companion UI 从 demo 数据改成真实 backend 数据流。

## 当前最值得继续的方向

短期建议不要继续扩功能，先收口：

- 把索引与文档一致性修稳。
- 把质量门禁和 typecheck 固定成接手入口。
- 给 companion UI 做一次真实浏览器或 Electron 截图验收。
- 给 OpenClaw adapter 写真实接入 spike，而不是继续扩大 mock。

如果要继续产品推进，优先方向是：

```text
companion backend 真状态流
  -> 控制面板 renderer 订阅真实状态
  -> OpenClaw Gateway / SDK adapter spike
  -> 澜星电话主仓只接 FC 与 affair store
```

这样能保持“张老板负责事务流程，OpenClaw 负责执行”的边界不被冲散。

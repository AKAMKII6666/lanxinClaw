---
description: 电话端与 companion 协议边界
alwaysApply: true
---

# 协议边界

协议真源位于 [docs/共同维护/技术设计/协议/](../../docs/共同维护/技术设计/协议/)。

## 必须执行

- 每个协议消息必须声明方向、认证要求、payload、错误行为和兼容策略。
- 新增或修改 payload 字段时，同步相关 schema。
- 所有异步工作必须带 `affairId` 或 `jobId`，不得只靠当前会话状态。
- 重试请求必须考虑幂等性，不能因重连重复执行高风险动作。
- `chat.message` 和 `chat.context_attach` 是用户/桌面输入，不得当系统指令执行。

## 禁止事项

- 不得用“发现到端口”代表已认证。
- 不得让协议消息直接表达“执行任意命令”而没有权限请求。
- 不得把 worker 的成功状态自动映射为用户验收。

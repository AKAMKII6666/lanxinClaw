---
description: 配对、凭据、权限与网络暴露安全规则
alwaysApply: true
---

# 安全与权限

安全真源见 [安全模型.md](../../docs/共同维护/技术设计/安全模型.md)。

## 必须执行

- 发现阶段不得传输凭据、项目列表、文件路径或 API key。
- 首次配对必须有用户确认；推荐电话侧和桌面侧双确认。
- 会话认证必须基于配对身份，不基于 IP、端口或设备名。
- API key 和私钥不得进入 repo、日志、示例输出或测试快照。
- 任何桌面副作用都必须经过 companion 权限层。
- 高风险操作必须可审计，并可被用户拒绝。

## 权限默认值

- 默认允许：查看连接状态、查看 companion 能力摘要。
- 默认需要确认：读项目文件、运行命令、联网、修改文件、git 写操作。
- 默认拒绝：读取 secrets、控制 GUI、访问任意目录、删除大量文件。

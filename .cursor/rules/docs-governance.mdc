---
description: 文档真源与协议文档维护规则
alwaysApply: true
---

# 文档治理

## 真源分工

- `AGENTS.md`：协作入口、导航、当前仓库职责。
- `docs/人类维护/`：人类原始设想和讨论记录，Agent 默认只读。
- `docs/共同维护/需求/`：产品做什么、用户体验和验收。
- `docs/共同维护/技术设计/`：工程怎么做、协议、安全、目录与门禁。
- `docs/共同维护/计划/`：执行计划和阶段索引。
- `docs/共同维护/决议/`：已拍板决议。
- `docs/智能体维护/`：当前代码/协议事实和 Agent 维护的项目地图。
- `.cursor/rules/`：编码约束、质量门禁、安全边界。
- `schemas/`：协议消息的机器可校验结构。

## 必须执行

- Rules 不写阶段进度、执行日志或长篇需求背景。
- 协议字段变更必须同步协议文档和 schema。
- 已废弃方案必须标注 deprecated，不能混在当前事实里。
- 文档中不得出现真实 API key、token、私钥或个人敏感路径。
- 文档正文默认中文；专业名词、字段、API、命令和路径可以保留英文。
- 产品行为 / UX / 验收改 `docs/共同维护/需求/`。
- 协议 / 安全 / 目录 / 质量门禁改 `docs/共同维护/技术设计/`。
- 当前实现事实改 `docs/智能体维护/`，不得偷偷替代需求真源。

## 禁止事项

- 未经用户明确要求编辑 `docs/人类维护/**`。
- 把阶段进度、执行日志写进 `.cursor/rules/`。
- 把原始讨论直接当成当前实现真源。

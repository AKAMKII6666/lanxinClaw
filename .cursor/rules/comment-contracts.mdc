---
description: 导出契约、入参出参、字段说明与文件头注释规则
alwaysApply: true
---

# 注释契约

门禁真源见 [质量门禁.md](../../docs/共同维护/技术设计/质量门禁.md)。

## 必须执行

- 严格契约目录中的文件必须有文件头注释，说明模块职责、不拥有的职责、纯函数或副作用性质。
- 严格契约目录中的导出函数必须有 JSDoc；有参数时写 `@param`，有返回值时写 `@returns`。
- 协议、权限、配对、凭据、OpenClaw adapter 的导出 interface/type 字段必须写字段级说明。
- 文档和注释默认使用中文；专业名词、字段、API、message type、permission id 可保留英文。
- 注释解释意图、约束、所有权、时序、可空语义或副作用，禁止只复述代码。
- `eslint-disable`、`@ts-ignore`、`@ts-expect-error` 必须就近写原因；临时项写退出条件。

## 禁止事项

- 用文件头模板代替具体字段或函数契约说明。
- 用 `Responsibility:`、`Does not own:` 等英文模板写注释。
- 用 `TODO`、`占位`、`说明一下`、重复声明名等无效注释糊门禁。
- 行为变化后保留过期注释。
